**System Initialization: Project Faber Workflow**

Before we write any code today, please ingest the current state of the project by reading the
following files:

* Read `SYSTEM_PROMPT.md` to understand the strict architectural constraints (Sandboxing, Jackson
  JSON parsing, HTTP 429 retry logic).
* Read `PROJECT_STATE.md` to understand what we have already accomplished and what our immediate
  next task is.
* Read `CODE_MAP.md` to load the current class signatures and interfaces into your context window.
  Do not guess class names or methods; rely strictly on this map.

Acknowledge that you have read these files by summarizing our exact next step. Wait for my
confirmation before generating any new Java files.